const CharacterTable: React.FC = () => {
    // Tutaj będziesz miał logikę do ładowania postaci
    const characters = []; // Dummy data
  
    return (
      <table className="table">
        {/* ... */}
      </table>
    );
};
  
export default CharacterTable;